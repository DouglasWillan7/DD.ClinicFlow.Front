# Execution status — identity, clinic hierarchy and consent v2

The canonical cross-repository plan is maintained at the ClinicFlow workspace root. This ledger is committed with frontend task changes because the workspace root and the applications are independent Git repositories.

| Task | Status | Gate evidence |
| --- | --- | --- |
| T41 — document-first web login and session | Complete | The web authenticates with country/type/document, safely handles authenticated versus clinic-selection outcomes, persists only contextual sessions, refreshes/logs out through v2, and recovers access using masked destinations plus opaque selections. Unit/component coverage includes validation, safe errors, keyboard/focus and recovery; `npm run lint`, `npm run build` and all 662 tests passed. The focused mobile Playwright gate passed 2/2 scenarios and verified no e-mail username, visible focus, keyboard operation, 44px targets and no 390px overflow. |
| T42 — secure clinic context selection and switching | Complete | The same accessible selector handles post-login selection and in-shell switching. A successful switch rotates the contextual session, removes the previous clinic's query/scoped cache and exposes only the active role/admin state; an unavailable target preserves the valid source context, while a suspended source clears the session. `npm run lint`, `npm run build` and all 671 tests passed. The focused Playwright gate passed and verified keyboard operation, 44px targets, no 390px overflow, token rotation, scoped-cache cleanup and clinic-bound profile contact copy. |
| T43 — membership-based team management | Complete | The team flow now uses the v2 membership contract: exactly one hierarchical role including Nurse, independent admin access, clinic-bound e-mail/phone, visible confirmation/status state and conditional doctor fields. Changing a doctor to another role requires explicit data-removal confirmation, owner admin/suspension controls are locked, and no reachable team editor submits a roles array. The backend safely exposes `isOwner` (`BACK 17f32b4`, all 1,568 tests passed). Frontend lint/build and all 678 tests passed; focused mobile Playwright verified keyboard operation, 44px targets and no 390px overflow. |
| T44 — capability-based web guards and navigation | Complete | A single capability matrix now mirrors all 18 backend capabilities and resolves the active `clinicRole` hierarchy independently from `isAdmin`; incomplete v2 contexts deny by default and legacy role fallback is isolated to sessions without `userClinicId`. Navigation, clinic switching, deep-route guards, patient/search destinations, schedule cards, doctor settings and transcription actions use capabilities. Secretary-admin can manage the clinic without seeing clinical actions, Nurse inherits operations without administration, and Doctor receives clinical navigation without implicit administration. Frontend lint/build and all 705 tests passed, including direct deep-route denial; the focused backend authorization matrix and endpoint policies passed 145/145, preserving server-side denial as the security boundary. |
| T45 — healthcare plans and appointment lifecycle | Complete | Appointment creation now lists only `HealthcarePlan` records accepted by the selected doctor-clinic membership, keeps private care available and derives duration from the doctor's returned slot. Canonical lifecycle states drive timeline/status copy: `AwaitingPatientAction` and `AccessRequired` remain visible but cannot open clinical content; starting a confirmed consultation first calls the appointment lifecycle, aborts on lost access, and only then starts transcription. Finishing drains transcription before completing the appointment, while scheduled and actual times remain distinct. Frontend lint/build and all 717 tests passed; focused backend lifecycle, creation and plan coverage passed 37/37. |
| T46 — standalone access and staff token confirmation | Complete | The patient edit flow now shows the Doctor→Patient access state to authorized doctors/administrators and creates idempotent standalone requests without parallel submissions. Agenda items in `AwaitingPatientAction` expose one accessible staff dialog that consumes the same Challenge used by the patient notification, so one token confirms both appointment and sharing; reissue respects cooldown, terminal/expired/declined/revoked states have explicit copy, and the minimal staff surface renders no clinical data. Backend support is in `530862e` (1,570/1,570 tests); frontend lint/build, 35 focused tests and all 727 tests passed. |
| T47 — public patient action and critical E2E | Complete | The anonymous `/acao-paciente/{reference}` page renders only a whitelist of the immutable snapshot: clinic, doctor, appointment, sharing explanation and terms version. It safely handles valid, completed/replayed, declined, expired, cancelled and invalid links; decline requires a second explicit action, while completion uses the backend's atomic link contract. Component/routing coverage passed 10/10 and the complete Vitest suite passed 733/733 with lint and production build green. Focused Playwright passed 2/2 at 390px, covering document login, keyboard clinic selection, atomic appointment+access completion, rollback on failure and revoked state with 44px targets/no overflow. The unrelated full legacy Playwright inventory remains 51 passed/54 failed because pre-v2 fixtures still depend on removed role and UI contracts; those failures were recorded and not weakened to satisfy this task. |
| T54 — reauthorization UAT and complete web gate | Complete | The complete browser inventory now exercises the deployed hierarchy instead of obsolete e-mail invitation/multiple-role fixtures: one role plus independent administration, clinic-bound contacts and doctor duration, explicit doctor-profile removal, clinical denial for Secretary/Admin-only sessions, the shared patient action and revoke/failure flows. A route-precedence regression was fixed so `/app/pacientes/novo` requires demographic management instead of being mistaken for a clinical-record route. `npm run lint`, all 733 Vitest tests, production build and all 98 Playwright scenarios passed; the browser gate completed in 43.32 seconds. |

**Next frontend task**: none before the cross-repository T55 enforcement rehearsal; T56 removes the remaining legacy contracts.
